    <div class="w-full">
        <label for="{{ $id }}"
            class="block mb-1 text-sm font-medium text-gray-900 dark:text-white">{{ $label }}</label>
        <select name="{{ $name }}" id="{{ $id }}" required>{{ $options }}</select>

    </div>
